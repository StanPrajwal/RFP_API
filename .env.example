# ----------------------------
# Server Configuration
# ----------------------------
PORT=8080                     # Port on which the server will run
MONGO_URI=                    # MongoDB connection string (no credentials committed)

# ----------------------------
# Azure OpenAI Configuration
# ----------------------------
AZURE_OPENAI_ENDPOINT=        # Your Azure OpenAI endpoint URL (e.g., https://xxx.openai.azure.com/)
AZURE_OPENAI_API_KEY=         # API key from Azure OpenAI (keep this secret)
AZURE_OPENAI_API_VERSION=     # API version used (e.g., 2024-02-01-preview)
AZURE_OPENAI_DEPLOYMENT=      # Azure deployment name (model deployment)
AZURE_OPENAI_MODEL=gpt-4o     # Model you want to use — keep as gpt-4o or change if needed

# ----------------------------
# Email SMTP Configuration
# ----------------------------
EMAIL_HOST=smtp.gmail.com     # SMTP server host
EMAIL_PORT=587                # SMTP port (587 = TLS)
EMAIL_USER=                    # Sender email address (no real email here)
EMAIL_PASS=                    # App password for email sending (never commit)

# ----------------------------
# Email IMAP Configuration
# ----------------------------
IMAP_HOST=imap.gmail.com      # IMAP server (for reading replies if needed)
IMAP_PORT=993                 # IMAP port (993 = SSL)
